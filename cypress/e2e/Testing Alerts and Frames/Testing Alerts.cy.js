const url = 'https://demoqa.com/alerts';


describe('Testing alert' , function () => {
    // test case
      it('should block alerts' , function () => {
    // launch the url
cy.visit(url);
    // click on submit button to produce the alert pop up
    // cy.get('input[type="submit"]').click();

describe('Testing timer alert' , function () => {
      it('should'              , function () => {                           
    //launch the url
    cy.visit(url);
    // click on submit button to produce the alert pop up
    // cy.get('input[type="submit"]').click();
})
describe('Confirm alert' , function () => {
    // test case
      it('should'        , function () => {
    // launch the url
    cy.visit(url);
    // click on submit button to produce the alert pop up
    // cy.get('input[type="submit"]').click();
    });
 });
})
    })

